<section>
  <header class="flex items-center justify-between">
    <h3 class="text-2xl font-semibold">Newest Pokémon Friends</h3>
    <%= render "shared/link", text: "View All", href: adoptable_pokemons_path, classname: 'text-end' %>
  </header>
  <ul class="embla overflow-hidden mt-10" id="recentAdoptablePokemon" data-controller="recent-pokemons">
    <li class="embla__container flex gap-x-4">
      <% @recent_adoptable_pokemons.each do |adoptable_pokemon| %>
        <%= render "adoptable_pokemons/adoptable_pokemon_badge", adoptable_pokemon: adoptable_pokemon %>
      <% end %>
    </li>
  </ul>
</section>
