<%= form_with do |form| %>
  <%= form.text_field(:q, placeholder: placeholder, value: params[:q], data: data, class: "placeholder:text-accent bg-transparent w-full lg:max-w-80 mt-auto px-4 py-2 !border-2 !border-accent text-accent rounded-lg font-semibold text-lg lg:col-start") %>
<% end %>